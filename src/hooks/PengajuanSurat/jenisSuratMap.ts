// import { z } from 'zod';

// export const jenisSuratMap = {
//   KETERANGAN_USAHA: 'Keterangan Usaha',
//   KETERANGAN_TIDAK_MAMPU_SEKOLAH: 'SKTM (Sekolah)',
// } as const;

// export const JenisSuratEnum = z.enum(Object.keys(jenisSuratMap) as [keyof typeof jenisSuratMap, ...Array<keyof typeof jenisSuratMap>]);
// export type JenisSuratKey = keyof typeof jenisSuratMap;